{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "rules.schema.json",
  "title": "Project rules",
  "description": "Project-wide gameplay rules (data/rules.json). Every field is optional; an absent file means all engine defaults.",
  "type": "object",
  "properties": {
    "check": {
      "type": "object",
      "description": "Skill-check rules.",
      "properties": {
        "dice": {
          "type": "string",
          "pattern": "^[1-9][0-9]*d([2-9]|[1-9][0-9]+)$",
          "description": "Default dice for active checks in NdM notation (e.g. 1d20, 2d6, 1d100). A per-check 'dice' field overrides this. Defaults to 1d20."
        },
        "criticals": {
          "type": "boolean",
          "description": "Critical rolls (snake-eyes / boxcars). When true, every die showing its minimum ALWAYS fails and every die showing its maximum ALWAYS succeeds, regardless of skill or difficulty. Defaults to false — enabling it changes every check outcome, so it is an explicit per-project opt-in."
        }
      },
      "additionalProperties": false
    },
    "quest": {
      "type": "object",
      "description": "Quest authoring rules.",
      "properties": {
        "tagVocabulary": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Controlled vocabulary for Quest.tags. Journal UIs group and prioritise quests from these, so a typo'd tag silently drops out of the grouping — declaring the vocabulary here turns that into an OBJ warning. Omit the field and any tag is accepted."
        }
      },
      "additionalProperties": false
    },
    "choices": {
      "type": "object",
      "description": "Choice presentation rules.",
      "properties": {
        "whenLockedDefault": {
          "type": "string",
          "enum": [
            "hide",
            "show"
          ],
          "description": "Project-wide default for a choice's 'whenLocked' when the choice does not set it. 'hide' (the engine default) keeps the classic behaviour — a choice whose showIf fails is dropped from the step; 'show' returns it in stepDialogue's lockedChoices for greyed-out presentation. A per-choice 'whenLocked' overrides this."
        }
      },
      "additionalProperties": false
    },
    "engine": {
      "type": "object",
      "description": "Engine-command rules (the 'engine' effect).",
      "properties": {
        "commands": {
          "type": "object",
          "description": "Declared engine command names, keyed by command. Declaring the set turns an undeclared command in an 'engine' effect into an ENGINE warning (a typo is otherwise a silent no-op at runtime). Omit the field and any name is accepted.",
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9_]*$"
          },
          "additionalProperties": {
            "type": "object",
            "properties": {
              "args": {
                "description": "Expected argument count, or \"any\". When a number, an effect whose args length differs is an ENGINE warning.",
                "oneOf": [
                  {
                    "type": "integer",
                    "minimum": 0
                  },
                  {
                    "const": "any"
                  }
                ]
              },
              "description": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },
    "flag": {
      "type": "object",
      "description": "Flag authoring rules.",
      "properties": {
        "exclusiveGroups": {
          "type": "array",
          "items": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 2
          },
          "description": "Lists of flags that are mutually exclusive. A validator rule enforces that no single effect sequence sets more than one flag in a group to true."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
