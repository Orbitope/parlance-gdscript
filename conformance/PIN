# The Parlance contract this port is pinned to.
#
# These vectors are a PINNED COPY, not a fork. Never hand-edit them: they are
# generated from and asserted against the reference implementation, so an edit
# here makes this port chase a ghost. Re-copy from the named ref instead.
parlanceVersion=v0.9.0-unreleased
parlanceCommit=1a4e6572e68064b3b6def073413660ef27648ae2
parlanceRepo=git@github.com:Orbitope/parlance.git
#
# v0.9.0 is prepared but NOT YET TAGGED upstream (blocked on Actions billing).
# INTEGRATION.md requires pinning an exact tag, so this pin is provisional and
# MUST be moved to v0.9.0 once that tag exists. Recorded rather than silently
# pinned to a moving branch.
