# The Parlance contract this port is pinned to.
#
# These vectors are a PINNED COPY, not a fork. Never hand-edit them: they are
# generated from and asserted against the reference implementation, so an edit
# here makes this port chase a ghost. Re-copy from the named ref instead.
# PRE-TAG: v0.15.0 is not tagged yet. The commit below is the release-prep
# commit on parlance branch claude/release-0-15-prep-mi2lm0, whose contract is
# what v0.15.0 will tag. Once the tag exists, move parlanceCommit to the
# v0.15.0 tag commit (re-copy and re-run if the contract moved) and delete
# this comment.
parlanceVersion=v0.15.0
parlanceCommit=440369d0af4a9f829a525843c16f7762e1d18d32
parlanceRepo=git@github.com:Orbitope/parlance.git
#
